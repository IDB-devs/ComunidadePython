from comunidadeimpressionadora import app


if __name__ == '__main__': #só vai rodar se tiver executanto esse arquivo especificamente
    app.run(debug=True) #roda o site em modo debug, atualiza automaticamente o código para o site
    
